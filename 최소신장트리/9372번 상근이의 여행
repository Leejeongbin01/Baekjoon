#include <iostream>
using namespace std;

int main() {
	int t, n, m;
	cin >> t;

	while (t--) {
		cin >> n >> m;
		for (int i = 0; i < m; i++) {
			int a, b;
			cin >> a >> b;
		}
		cout << n - 1 << "\n";
	}
	return 0;
}
