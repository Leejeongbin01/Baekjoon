#include <iostream>
using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(NULL);
    cout.tie(NULL);

    int numBuyer;
    cin >> numBuyer;
    for(int i=0; i<numBuyer; i++){
        int eachNum; cin >> eachNum;
        for(int j=0; j<eachNum; j++){
            int a,b; cin >> a >> b;

        }
    }

    int M; cin >> M;
    while(M--){
        int Q; cin >> Q;

        
    }

    return 0;
}