#include <iostream>
using namespace std;

int N,K;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(NULL);
    cout.tie(NULL);

    cin >> N >> K;
    for(int i=0; i<N; i++){
        
    }

    return 0;
}